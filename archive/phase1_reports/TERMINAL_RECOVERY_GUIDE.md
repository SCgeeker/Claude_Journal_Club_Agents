# PowerShell 終端恢復指南

## 問題診斷結果

✅ **已確認問題**：
- PowerShell 終端卡住，無任何輸出
- 發現 Python 進程 PID: 15108 (PowerShell)
- 測試腳本 `test_parse_single_zettel.py` 可能卡在輸出緩衝

---

## 🚀 立即修復方案

### 方案 A：強制終止進程（推薦）

**步驟 1：在新的 PowerShell 或 CMD 視窗執行**

```powershell
# 方法 1：使用 taskkill（推薦）
taskkill /IM python.exe /F

# 方法 2：使用 PowerShell
Get-Process python | Stop-Process -Force

# 方法 3：指定 PID（如果知道具體 PID）
taskkill /PID 22248 /F
```

**步驟 2：回到卡住的終端**
- 按 **Enter** 鍵
- 應該會看到 "已終止" 或錯誤訊息
- 如果還是沒反應，繼續下一步

**步驟 3：清理終端狀態**
```powershell
# 輸入任意命令測試
dir

# 如果正常，重新執行測試
python test_parse_quick.py
```

---

### 方案 B：重啟 PowerShell 終端

**如果方案 A 無效，直接重啟終端：**

1. **關閉卡住的 PowerShell 終端**
   - 點擊 X 關閉視窗
   - 或按 Alt+F4

2. **開啟新的 PowerShell 終端**
   ```powershell
   cd D:\core\research\claude_lit_workflow
   ```

3. **使用改良版測試腳本**
   ```powershell
   python test_parse_quick.py
   ```

---

## 📊 當前系統狀態

根據診斷結果：

| 項目 | 狀態 | 說明 |
|------|------|------|
| Python 進程 | ⚠️ 1 個 | 可能卡住 |
| PowerShell 進程 | ✅ 1 個 | PID 15108 |
| 測試腳本 | ⚠️ 卡住 | 超過 10 分鐘無輸出 |
| 知識庫 | ✅ 正常 | 已驗證可用 |

---

## 🔧 根本原因分析

### 為什麼會卡住？

1. **輸出緩衝問題** ⭐ 最可能
   - `test_parse_single_zettel.py` 使用 `print()` 沒有 `flush=True`
   - Windows PowerShell 的輸出緩衝機制
   - 導致輸出累積在緩衝區，不顯示在終端

2. **進程等待輸入**
   - 可能某處有 `input()` 調用
   - 用戶看不到提示訊息

3. **YAML 解析錯誤**
   - 某些卡片格式不標準
   - 導致解析器進入無限循環

---

## ✅ 已準備的解決方案

### 改良版測試腳本：`test_parse_quick.py`

**特點**：
- ✅ 強制實時輸出 (`flush=True`)
- ✅ 進度顯示 (1/4, 2/4, 3/4, 4/4)
- ✅ 詳細錯誤處理
- ✅ 執行時間統計
- ✅ 已在本終端測試成功（0.05 秒）

**使用方式**：
```bash
python test_parse_quick.py
```

**預期輸出**：
```
============================================================
快速測試：Zettelkasten 卡片解析
============================================================

[1/4] 檢查測試卡片是否存在...
✅ 找到測試卡片: Linguistics-20251029-001.md

[2/4] 讀取卡片內容...
✅ 讀取成功 (819 字元)

[3/4] 檢查文件格式...
✅ YAML frontmatter 存在
✅ 連結網絡區塊存在
✅ 核心概念和 AI 筆記存在

[4/4] 測試知識庫解析器...
✅ 知識庫模組導入成功
✅ 知識庫管理器初始化成功

✅ 解析成功！

解析結果摘要:
  - zettel_id: Linguistics-20251029-001
  - title: "Mass Noun (Mass Noun)"
  - domain: Linguistics
  - tags: 3 個
  - links: 1 組

============================================================
測試完成
============================================================

總耗時: 0.05 秒
```

---

## 🎯 恢復工作流程

### 修復後的工作步驟：

1. **終止卡住的進程** ✅ 執行方案 A 或 B

2. **驗證系統狀態**
   ```bash
   python check_processes.py
   ```

3. **執行快速測試**
   ```bash
   python test_parse_quick.py
   ```

4. **繼續原計劃的工作**
   - 如果你在進行 Zettelkasten 索引測試
   - 可以繼續使用 `test_zettel_indexing.py`

5. **批次索引（如需要）**
   ```bash
   python test_zettel_indexing.py
   ```

---

## 📝 預防未來卡住

### 修改原測試腳本（可選）

如果你還想使用 `test_parse_single_zettel.py`，需要修改：

**在文件開頭添加**：
```python
import functools
# 強制所有 print 都實時輸出
print = functools.partial(print, flush=True)
```

或手動在每個 print 添加 `flush=True`：
```python
print("訊息", flush=True)
```

---

## 🆘 如果還是無法修復

### 最後手段：

1. **重啟 Windows 終端應用**
   - 關閉所有 PowerShell 視窗
   - 重新開啟

2. **檢查 Python 環境**
   ```bash
   python --version
   where python
   ```

3. **重新初始化知識庫**
   ```bash
   python -c "from src.knowledge_base import KnowledgeBaseManager; kb = KnowledgeBaseManager(); print(kb.get_stats())"
   ```

4. **聯繫支援**
   - 提供 `check_processes.py` 的輸出
   - 提供卡住時的截圖
   - 說明執行的具體命令

---

## 📞 快速命令參考

```bash
# 檢查進程
python check_processes.py

# 診斷終端
python recover_terminal.py

# 自動修復
python recover_terminal.py --kill

# 快速測試
python test_parse_quick.py

# 批次索引測試
python test_zettel_indexing.py

# 知識庫統計
python -c "from src.knowledge_base import KnowledgeBaseManager; kb = KnowledgeBaseManager(); print(kb.get_stats())"
```

---

**建立時間**: 2025-10-30 16:01
**最後更新**: 2025-10-30 16:05
**狀態**: ✅ 已準備修復方案

# Metadata 修復指南

**工具**: `fix_metadata.py`
**目的**: 自動修復知識庫論文的缺失元數據（年份、關鍵詞、摘要）
**狀態**: ✅ 可用（v1.0）

---

## 📊 快速統計

**當前知識庫問題**（31篇論文）:
- ❌ 缺少年份: 30 篇（96.8%）
- ❌ 關鍵詞不足: 20 篇（64.5%）
- ❌ 摘要缺失: 16 篇（51.6%）

**自動修復成功率**（測試結果）:
- ✅ 年份修復: **65%**（15/23 篇成功）
- ⚠️ 關鍵詞修復: **待測試**（需要高質量內容）
- ⚠️ 摘要修復: **待測試**（需要標準格式）

---

## 🚀 快速開始

### 1. 列出需要修復的論文

```bash
# 列出所有有問題的論文
python fix_metadata.py --list

# 只列出缺少年份的論文
python fix_metadata.py --list --field year

# 只列出缺少關鍵詞的論文
python fix_metadata.py --list --field keywords

# 只列出缺少摘要的論文
python fix_metadata.py --list --field abstract
```

**輸出範例**:
```
找到 30 篇需要修復的論文
================================================================================

ID 1: Taxonomy of Numeral Classifiers:
  年份: 缺失
  關鍵詞: 缺失
  摘要: 缺失

ID 2: Chinese Classifiers and Count Nouns
  年份: 缺失
  關鍵詞: ["Count Nouns", "Quantifier", ...]
  摘要: 缺失
...
```

### 2. 預覽修復（不實際執行）

```bash
# 預覽前 5 篇論文的修復
python fix_metadata.py --batch --dry-run --limit 5

# 預覽所有年份修復
python fix_metadata.py --batch --field year --dry-run
```

**輸出範例**:
```
找到 5 篇需要修復的論文
⚠️ 預覽模式（不會實際修改）

[1/5] 論文 ID 1: Taxonomy of Numeral Classifiers:...
  年份: None → 2007
  關鍵詞: None → ['classifiers', 'numeral', 'taxonomy']... (5 個)
...
```

### 3. 修復單篇論文

```bash
# 修復論文 ID 4
python fix_metadata.py --paper-id 4
```

**輸出範例**:
```
修復論文 ID 4...

✅ 修復成功!
論文: Concepts in the Brain
  - year: 2019
```

### 4. 批次修復

```bash
# 修復所有缺少年份的論文
python fix_metadata.py --batch --field year

# 修復所有有問題的論文（年份、關鍵詞、摘要）
python fix_metadata.py --batch

# 限制修復數量（測試用）
python fix_metadata.py --batch --field year --limit 10
```

**輸出範例**:
```
找到 23 篇需要修復的論文

[1/23] 論文 ID 1: Taxonomy of Numeral Classifiers:...
  ✅ 修復成功
    - year: 2007

[2/23] 論文 ID 2: Chinese Classifiers and Count Nouns...
  ✅ 修復成功
    - year: 2009

[3/23] 論文 ID 5: 華語分類詞的界定與教學上的分級 1...
  ❌ 失敗: 未知錯誤

...

================================================================================
批次修復結果
================================================================================
總計: 23
成功: 15
失敗: 8
無更新: 0
```

---

## 🔍 提取策略說明

### 年份提取（5 種策略）

| 優先級 | 策略 | 範例 | 成功率 |
|--------|------|------|--------|
| 1 | "Year: YYYY" 或 "- 年份: YYYY" | `Year: 2023` | 高 |
| 2 | 版權聲明（© ... YYYY） | `© Oxford University Press 2019` | 中高 |
| 3 | (YYYY) 在標題或作者附近 | `Smith (2020)` | 中 |
| 4 | 從標題提取 | `Journal2022;8(1):151-164` | 中 |
| 5 | 獨立的 YYYY | `Published in 2015` | 低 |

**範圍限制**: 1950-2030（避免錯誤匹配）

### 關鍵詞提取（2 種策略）

| 優先級 | 策略 | 範例 | 限制 |
|--------|------|------|------|
| 1 | "Keywords:" 後的內容 | `Keywords: NLP, AI, Deep Learning` | 需要明確標註 |
| 2 | 提取加粗術語 | `**machine learning**` | 可能不準確 |

**過濾規則**:
- 移除 YAML front matter（避免提取到元數據）
- 長度限制：2-50 字元
- 排除無效內容（如 "created:", "---"）
- 最多保留 10 個關鍵詞

### 摘要提取（3 種策略）

| 優先級 | 策略 | 範例 | 限制 |
|--------|------|------|------|
| 1 | "Abstract" 標題後的內容 | `## Abstract\n\nThis paper...` | 需要標準格式 |
| 2 | "摘要" 標題後的內容 | `## 摘要\n\n本文...` | 需要中文標題 |
| 3 | 首段（第一個 ## 前） | 文章開頭的第一段落 | 可能不是摘要 |

**長度限制**: 100-2000 字元

---

## ⚙️ 命令參數說明

| 參數 | 類型 | 說明 | 範例 |
|------|------|------|------|
| `--list` | 標記 | 列出需要修復的論文 | `--list` |
| `--field` | 選項 | 指定字段（year/keywords/abstract） | `--field year` |
| `--paper-id` | 整數 | 修復單篇論文 | `--paper-id 4` |
| `--batch` | 標記 | 批次修復 | `--batch` |
| `--limit` | 整數 | 限制修復數量 | `--limit 10` |
| `--dry-run` | 標記 | 預覽模式（不實際修改） | `--dry-run` |

---

## 📈 使用建議

### 推薦工作流

#### **階段 1: 評估問題**（5 分鐘）
```bash
# 1. 查看所有問題
python fix_metadata.py --list

# 2. 分別查看各字段問題
python fix_metadata.py --list --field year
python fix_metadata.py --list --field keywords
python fix_metadata.py --list --field abstract
```

#### **階段 2: 預覽修復**（10 分鐘）
```bash
# 1. 預覽年份修復
python fix_metadata.py --batch --field year --dry-run --limit 10

# 2. 檢查提取質量
#    - 年份是否合理？
#    - 關鍵詞是否相關？
#    - 摘要是否準確？
```

#### **階段 3: 批次修復**（15 分鐘）
```bash
# 1. 先修復年份（最穩定）
python fix_metadata.py --batch --field year

# 2. 檢查結果
python check_quality.py --min-score 60

# 3. 如果成功率 >= 60%，繼續修復關鍵詞
python fix_metadata.py --batch --field keywords --limit 10
python fix_metadata.py --batch --field keywords  # 如果效果好

# 4. 最後修復摘要（可選）
python fix_metadata.py --batch --field abstract --limit 5
```

#### **階段 4: 手動修復失敗項**（30 分鐘）
```bash
# 1. 列出仍有問題的論文
python fix_metadata.py --list

# 2. 手動檢查和編輯 Markdown 文件
#    - 查看原始 PDF（如果可用）
#    - 使用 Google Scholar 或 CrossRef 查詢
#    - 直接編輯 knowledge_base/papers/*.md

# 3. 使用 SQLite 直接更新（高級）
sqlite3 knowledge_base/index.db
UPDATE papers SET year = 2019 WHERE id = 5;
UPDATE papers SET keywords = '["AI", "NLP"]' WHERE id = 7;
```

### 成功率優化建議

| 問題 | 原因 | 解決方案 |
|------|------|----------|
| 年份提取失敗 | 非標準格式、書籍、無日期 | 手動查詢 Google Scholar 或 CrossRef |
| 關鍵詞提取失敗 | 無明確標註、格式不標準 | 使用 LLM 生成（見下方） |
| 摘要提取失敗 | 無 Abstract 標題、書籍目錄 | 使用 LLM 生成或手動編寫 |

---

## 🤖 高級方法：使用 LLM 生成 Metadata

對於自動提取失敗的論文，可以使用 LLM 從內容生成高質量 metadata。

### 方法 1: 使用 Ollama（本地免費）

```bash
# 1. 確保 Ollama 運行中
ollama serve

# 2. 使用 gemma2 生成關鍵詞
ollama run gemma2 "根據以下論文內容提取5-10個關鍵詞（英文），用逗號分隔：

$(cat knowledge_base/papers/Concepts_in_the_Brain.md | head -100)
"

# 3. 使用 gemma2 生成摘要
ollama run gemma2 "根據以下論文內容撰寫150-200字的摘要（中文）：

$(cat knowledge_base/papers/Concepts_in_the_Brain.md | head -200)
"
```

### 方法 2: 使用 Google Gemini（雲端，快速便宜）

```python
# 創建 llm_metadata_fixer.py

import google.generativeai as genai
import os

genai.configure(api_key=os.environ['GOOGLE_API_KEY'])
model = genai.GenerativeModel('gemini-2.0-flash-exp')

# 讀取論文內容
with open('knowledge_base/papers/Concepts_in_the_Brain.md', 'r', encoding='utf-8') as f:
    content = f.read()[:5000]  # 前5000字元

# 生成關鍵詞
prompt_keywords = f"""
請從以下論文內容提取5-10個關鍵詞（英文），用逗號分隔。
只返回關鍵詞列表，不要其他內容。

論文內容：
{content}
"""

response = model.generate_content(prompt_keywords)
keywords = [kw.strip() for kw in response.text.split(',')]
print(f"關鍵詞: {keywords}")

# 生成摘要
prompt_abstract = f"""
請為以下論文內容撰寫一段學術摘要（繁體中文），150-200字，
包含：研究目的、方法、主要發現、結論。

論文內容：
{content}
"""

response = model.generate_content(prompt_abstract)
abstract = response.text
print(f"摘要: {abstract}")
```

執行：
```bash
python llm_metadata_fixer.py
```

**成本估算**: ~$0.0001/篇（Google Gemini，極低）

---

## 🔧 故障排除

### 問題 1: 修復失敗（❌ 失敗: 未知錯誤）

**原因**:
- Markdown 文件無法提取任何 metadata
- 文件內容為空或格式異常

**解決方案**:
```bash
# 1. 手動檢查文件
cat knowledge_base/papers/<file>.md | head -100

# 2. 查看原始 PDF（如果可用）
# 3. 使用 LLM 生成（見上方高級方法）
# 4. 手動編輯 Markdown 文件
```

### 問題 2: 提取的年份錯誤

**範例**: 提取到 2015 但實際應該是 2020

**原因**:
- 引用的文獻年份
- 版權聲明的舊年份

**解決方案**:
```bash
# 1. 手動更新數據庫
sqlite3 knowledge_base/index.db
UPDATE papers SET year = 2020 WHERE id = 5;

# 2. 或直接編輯 Markdown YAML front matter
---
year: 2020
---
```

### 問題 3: 關鍵詞提取到無效內容

**範例**: `['created: 2025-10-29', '---', 'title']`

**原因**: YAML front matter 沒有被正確過濾

**解決方案**: 已在 v1.0 修復（移除 YAML front matter）

### 問題 4: Windows 編碼錯誤

**錯誤**: `UnicodeEncodeError: 'cp950' codec can't encode character`

**解決方案**: 已內建 UTF-8 支援（自動處理）

---

## 📊 測試結果（2025-11-01）

### 年份修復測試

| 測試範圍 | 總數 | 成功 | 失敗 | 成功率 |
|---------|------|------|------|--------|
| 前 10 篇 | 10 | 6 | 4 | 60% |
| 全部 | 23 | 15 | 8 | **65%** |

**成功案例**:
- ID 1: Taxonomy → 2007 ✅
- ID 2: Chinese Classifiers → 2009 ✅
- ID 3: LanguageSciences2003 → 2003 ✅
- ID 4: Concepts in the Brain → 2019 ✅（版權聲明）
- ID 6: Numeral classifiers → 2023 ✅
- ID 8: LinguisticsVanguard2022 → 2022 ✅（標題）

**失敗案例**:
- ID 5: 華語分類詞 ❌（中文內容，格式特殊）
- ID 7: International Journal ❌（標題不完整）
- ID 11: https://doi.org/... ❌（標題為 URL）
- ID 12: Events as Intersecting ❌（需檢查內容）

### 關鍵詞和摘要修復

**狀態**: 待測試（預期成功率 30-50%）

---

## 🎯 後續改進計畫

### 短期（1-2天）
- [x] 年份提取優化（5 種策略）
- [x] 關鍵詞過濾改進（移除 YAML front matter）
- [ ] 測試關鍵詞和摘要修復
- [ ] 創建 LLM 輔助修復腳本

### 中期（1週）
- [ ] 整合外部 API（CrossRef、Semantic Scholar）
- [ ] DOI 查詢功能
- [ ] 自動下載元數據
- [ ] 批次驗證修復結果

### 長期（1個月）
- [ ] 機器學習模型訓練（領域特定關鍵詞提取）
- [ ] 自動摘要生成（使用 LLM）
- [ ] 互動式修復介面（CLI 或 Web）
- [ ] 元數據質量評分系統

---

## 📚 相關文檔

- `check_quality.py`: 檢查元數據質量
- `OPTIMIZATION_REPORT_20251101.md`: 優化報告
- `quality_report.txt`: 質量檢查結果
- `src/checkers/quality_rules.yaml`: 質量規則定義

---

## 💡 最佳實踐

### DO ✅
- ✅ 先預覽再批次修復（`--dry-run`）
- ✅ 分字段修復（年份 → 關鍵詞 → 摘要）
- ✅ 限制數量測試（`--limit 10`）
- ✅ 修復後檢查質量（`check_quality.py`）
- ✅ 備份數據庫（`cp knowledge_base/index.db knowledge_base/index.db.bak`）

### DON'T ❌
- ❌ 不驗證直接批次修復全部
- ❌ 不檢查提取質量
- ❌ 不備份數據庫
- ❌ 忽略失敗案例（需手動處理）

---

**文檔版本**: 1.0
**最後更新**: 2025-11-01
**狀態**: ✅ 可用
**成功率**: 年份 65% | 關鍵詞/摘要 待測試
